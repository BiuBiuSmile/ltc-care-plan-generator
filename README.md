# 個管照顧計畫 GitHub Pages v9.2 管理者直通版

- 測試者：Email 驗證，最多 10 位，每位 10 次 AI。
- 管理者：先在 `admin.html` 用 `CARE_PLAN_ADMIN_KEY` 登入，再按「進入計畫系統」。
- 同一瀏覽器分頁工作階段會自動辨識管理者，不需要 Email 驗證。
- 管理者 AI 呼叫不占 10 位測試名額，也不扣測試者 10 次額度。
- 管理密碼不會放在網址；關閉分頁/瀏覽器工作階段後需重新由管理頁登入。
